name: route
description: Route package
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  deliveries:
    path: ../deliveries
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
